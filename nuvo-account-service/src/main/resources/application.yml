server:
  port: 8082

spring:
  application:
    name: account-service
  datasource:
    # Base de datos DIFERENTE: nuvo_account_db
    url: jdbc:mysql://localhost:3306/nuvo_account_db?createDatabaseIfNotExist=true&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
    username: root      # <--- CAMBIA SI ES NECESARIO (ej: nuvo_dev)
    password: 1234     # <--- CAMBIA POR TU CONTRASEÑA REAL
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect