server:
  port: 8084

spring:
  application:
    name: loan-service
  datasource:
    url: jdbc:mysql://localhost:3306/nuvo_loan_db?createDatabaseIfNotExist=true&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
    username: root      # <--- TU USUARIO
    password: 1234      # <--- TU PASSWORD
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect